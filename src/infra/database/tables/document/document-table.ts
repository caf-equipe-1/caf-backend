export const documentTable: string = `
    CREATE TABLE document(
        id varchar(50),
        name varchar(100),
        document TEXT,
        createdAt varchar(10),
        updatedAt varchar(10)
    )
`;
